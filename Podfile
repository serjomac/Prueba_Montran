# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'pruebamontrant' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for pruebamontrant
  pod 'Alamofire', '~> 5.4'

  target 'pruebamontrantTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'pruebamontrantUITests' do
    # Pods for testing
  end

end
